# Railway.com Environment Variables Template
# Copy these values to Railway dashboard: railway variables set KEY=value

# ===== REQUIRED VARIABLES =====

# Database Configuration (Supabase recommended)
DATABASE_URL=postgresql://postgres:password@host:5432/postgres
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Application Configuration
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://your-frontend-domain.com

# ===== OPTIONAL VARIABLES =====

# Redis Configuration (Railway Redis or Upstash)
REDIS_URL=redis://username:password@host:port

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@pricepulse.com

# Authentication (if using legacy auth)
JWT_SECRET=your-jwt-secret-here
USE_SUPABASE_AUTH=true

# Feature Flags
REALTIME_ENABLED=true
STORAGE_ENABLED=true
EMAIL_NOTIFICATIONS_ENABLED=true

# Scraping Configuration
SCRAPING_DELAY=1000
MAX_CONCURRENT_SCRAPES=3
SCRAPING_TIMEOUT=30000

# ===== RAILWAY-SPECIFIC NOTES =====
# 
# 1. Railway automatically sets PORT - don't override it
# 2. Use Railway PostgreSQL or Supabase for database
# 3. Use Railway Redis or Upstash for cache
# 4. Set FRONTEND_URL to your actual frontend domain
# 5. For production, use strong JWT_SECRET (generate with: openssl rand -base64 32)
#
# Set variables using Railway CLI:
# railway variables set DATABASE_URL=your-database-url
# railway variables set SUPABASE_URL=your-supabase-url
# railway variables set SUPABASE_ANON_KEY=your-anon-key
