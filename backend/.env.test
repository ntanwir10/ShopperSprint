# Test Environment Configuration
NODE_ENV=test

# Test Database (in-memory or separate test database)
DB_HOST=localhost
DB_PORT=5432
DB_USER=pricetracker
DB_PASSWORD=pricetracker123
DB_NAME=price_tracker_test

# Test Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Test Configuration
TEST_TIMEOUT=10000
LOG_LEVEL=error
